# Vocab

See [CONTEXT.md](./CONTEXT.md) for what this project is and the domain vocabulary it speaks in.

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues on `dudujuju828/vocab-builder`, driven through the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, used verbatim as label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` and one `docs/adr/` at the repo root. See `docs/agents/domain.md`.
